import React from "react";

const Interview = (props) => {
    return (
        <div className="interview">
            <p className="interviewText">{props.text}</p>
        </div>
    )
}

export default Interview;